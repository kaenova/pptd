# Python-generated files
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# macOS metadata
.DS_Store

# Packaged artifacts
*.tgz
*.zip

# Local skill install metadata
_user_meta.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# QA reference PNGs — regenerable via `python3 -m pptd_utils png all <deck>.pptd`
skills/cowork-ppt/example/**/*-png/

# Agent Harness Directory
.pi